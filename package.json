{
  "homepage": "https://jxie93.github.io/missile-command-nodejs",
  "name": "missile-command-phaser3",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider http-server",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider webpack",
    "watch": "cross-env NODE_OPTIONS=--openssl-legacy-provider webpack --watch",
    "predeploy": "npm run build",
    "deploy": "cross-env NODE_OPTIONS=--openssl-legacy-provider gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.8.0",
    "webpack": "^4.8.3",
    "webpack-cli": "^2.1.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "gh-pages": "^6.3.0",
    "http-server": "^0.11.1",
    "ts-loader": "^4.3.0",
    "typescript": "^2.8.3"
  }
}
